import ChatPage from "./Chat";

const ChatIdPage = () => {
  // This will be replaced with logic to load chat by ID
  return <ChatPage />;
};

export default ChatIdPage;

/**
 * Usage:
 * import ChatIdPage from "./ChatId.tsx";
 * <ChatIdPage />
 */
